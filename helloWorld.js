const greet = (name) => {
    console.log("Hello", name)
}

greet("Amie");
greet("Jabu")

const name = "Lansanaamaralengor";

const myArray = ["Please", "push", "me", "like", "kush"]
for (var x = 0; x < myArray.length; x++ ) {
    const name = "lengorlansana";
    console.log(myArray[x]);
}
